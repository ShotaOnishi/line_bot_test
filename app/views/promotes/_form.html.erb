<%= form_for(promote) do |f| %>
  <% if promote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promote.errors.count, "error") %> prohibited this promote from being saved:</h2>

      <ul>
      <% promote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
